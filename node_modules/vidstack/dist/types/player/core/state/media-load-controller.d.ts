import { ComponentController, ComponentInstance } from 'maverick.js/element';
import type { PlayerAPI } from '../../player';
export declare class MediaLoadController extends ComponentController<PlayerAPI> {
    private _callback;
    constructor(instance: ComponentInstance<PlayerAPI>, _callback: () => void);
    protected onAttach(el: HTMLElement): Promise<(() => void) | undefined>;
}
