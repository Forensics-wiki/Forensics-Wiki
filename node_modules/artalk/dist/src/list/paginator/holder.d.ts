import { IPgHolderConf, TPgMode } from '.';
/**
 * 分页方式持有者（调度器）
 */
export default class PgHolder {
    private conf;
    constructor(conf: IPgHolderConf);
    getAdaptor(): import("./adaptors").IPgAdaptor<any>;
    init(): void;
    setLoading(val: boolean): void;
    update(offset: number, total: number): void;
    getEl(): HTMLElement;
    showErr(msg: string): void;
    setMode(val: TPgMode): void;
    next(): void;
}
