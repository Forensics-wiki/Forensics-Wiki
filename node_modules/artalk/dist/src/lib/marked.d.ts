import { marked as libMarked } from 'marked';
import Context from '../../types/context';
export type TMarked = typeof libMarked;
/** Get Marked instance */
export declare function getInstance(): typeof libMarked | undefined;
/** 初始化 marked */
export declare function initMarked(): void;
/** 解析 markdown */
export default function marked(ctx: Context, src: string): string;
