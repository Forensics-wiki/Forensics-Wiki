import Editor from './editor';
export interface Mover {
    editor: Editor;
    isMoved: boolean;
    move(afterEl: HTMLElement): void;
    back(): void;
}
export declare function createMover(editor: Editor): Mover;
