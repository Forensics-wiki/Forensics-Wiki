declare const CSS: unique symbol;
export type CSS = {
    readonly [CSS]?: true;
    readonly text: string;
    readonly sheet?: CSSStyleSheet;
    toString(): string;
};
export declare function injectCSS(value: unknown): CSS;
/**
 * A template literal tag which can be used with Maverick's `declareCustomElement` function to
 * create element styles. For security reasons, only literal string values and number may be used in
 * embedded expressions. Use `injectCSS` to incorporate non-literal values inside CSS expressions.
 */
export declare function css(strings: TemplateStringsArray, ...values: (CSS | number)[]): CSS;
export interface StylesheetAdopter {
    (root: Document | ShadowRoot, css: CSS[]): any;
}
export declare function adoptCSS(root: Document | ShadowRoot, css: CSS[]): void;
export {};
