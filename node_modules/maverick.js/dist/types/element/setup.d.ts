import type { HTMLCustomElement } from './host';
/**
 * This function is dynamically imported and used to setup when there is no delegate (i.e., no
 * host framework such as loading over a CDN).
 */
export declare function setup(host: HTMLCustomElement<any>): Promise<void>;
export declare function resolvePropsFromAttrs(host: HTMLCustomElement<any>): Record<string, any>;
export declare function findParent(host: HTMLCustomElement<any>): HTMLCustomElement<any> | null;
